# Build tooling (NestJS CLI, TypeScript, @types/*) lives in devDependencies.
# Force npm to install them even when NODE_ENV=production (e.g. on Render),
# otherwise `nest build` fails with "nest: not found".
include=dev
